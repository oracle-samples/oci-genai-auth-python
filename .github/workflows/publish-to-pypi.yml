name: Publish Python Package

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  release-build:
    runs-on: ubuntu-latest
    # PyPI use trusted publisher    
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv
          uv venv
          make dev
      - name: Build
        run: |
          make build
      - name: Validate
        run: |
          WHEEL=$(ls dist/*.whl | head -n 1)
          python -m pip install "${WHEEL}[openai,google]"
          python -c "from oci_genai_support.openai import OciOpenAI; from oci_genai_support.google import OciGoogleGenAI; import oci_genai_support;"
      # - name: Publish to Test PyPI
      #   run: |
      #     python -m pip install twine
      #     twine check dist/*
      #     twine upload --verbose -r testpypi dist/*
      - name: Publish to PyPI
        run: |
          python -m pip install twine
          twine check dist/*
          twine upload --verbose dist/*
